// explore.js

window.addEventListener('DOMContentLoaded', init);

function init() {
  // TODO
  
  // get all of the aspects from the html
  // button
  const talkButton = document.querySelector("button");
  // text box
  const talkTextBox = document.querySelector("textarea")
  // image
  const talkImage = document.querySelector("img");
  // dropbox
  const talkSelection = document.querySelector("select");

  // adds the options to dropbox
  talkSelection.addEventListener('DOMContentLoaded', function() {

  });
}
